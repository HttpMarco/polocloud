---
title: Events
description: Understanding events in PoloCloud and how to manage them with the Rust SDK.
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';
import { Callout } from 'fumadocs-ui/components/callout';

## What are Events?

In PoloCloud, **Events** represent real-time notifications about changes in your cloud infrastructure. Events allow you to react to player actions, service state changes, and other important occurrences.

<div className="text-center mb-12">
    <div className="w-20 h-1 bg-gradient-to-r from-transparent via-primary/50 to-transparent rounded-full mx-auto mb-6" />
    <div className="text-xl text-muted-foreground max-w-3xl mx-auto">
        React to real-time changes in your cloud infrastructure
    </div>
</div>

### Event Types

- **Player Events** - Player joins, leaves, moves between services
- **Service Events** - Service starts, stops, crashes
- **Group Events** - Group creation, deletion, configuration changes
- **System Events** - Infrastructure changes, maintenance

## Working with Events

### Getting the Event Provider

First, get access to the event provider using the PoloCloud Rust SDK:

<DynamicCodeBlock lang="rust" code={`use polocloud_sdk::PoloCloudSDK;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let sdk = PoloCloudSDK::new().await?;
    let event_provider = sdk.event_provider();
    
    // Your event operations here
    
    Ok(())
}`} />

### Subscribing to Events

Subscribe to events with custom event types:

<DynamicCodeBlock lang="rust" code={`use polocloud_sdk::PoloCloudSDK;
use serde::Deserialize;

// Define your event type
#[derive(Debug, Deserialize)]
struct PlayerJoinedEvent {
    player_name: String,
    service_name: String,
}

// Implement the Event trait
impl polocloud_sdk::Event for PlayerJoinedEvent {
    fn event_name() -> String {
        "player_joined".to_string()
    }
}

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let sdk = PoloCloudSDK::new().await?;
    let mut event_provider = sdk.event_provider().lock().await?;
    
    // Subscribe to events
    event_provider.subscribe::<PlayerJoinedEvent, _>(|event| {
        println!("Player {} joined service {}", event.player_name, event.service_name);
    }).await?;
    
    // Keep the program running to receive events
    tokio::time::sleep(tokio::time::Duration::from_secs(60)).await;
    
    Ok(())
}`} />

### Event Trait

Your event types must implement the `Event` trait:

<DynamicCodeBlock lang="rust" code={`use polocloud_sdk::Event;

impl Event for MyEvent {
    fn event_name() -> String {
        "my_event".to_string()
    }
}`} />

<Callout type="tip">
**Best Practice**

Use events to build reactive systems that respond to changes in your infrastructure. This enables real-time monitoring, logging, and automated responses.
</Callout>