---
title: Find Services by Group Name
description: Find services by their group name using the Rust SDK.
---

import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';
import { Callout } from 'fumadocs-ui/components/callout';

## Find services by group name

Use this method to find all services belonging to a specific group.

<DynamicCodeBlock
  lang="rust"
  code={`use polocloud_sdk::PoloCloudSDK;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    let sdk = PoloCloudSDK::new().await?;
    let mut service_provider = sdk.service_provider().lock().await?;
    
    let group_name = "lobby".to_string();
    let services = service_provider.find_group_name_async(group_name).await?;
    
    for service in services {
        println!("Found service: {} ({}:{})", 
                service.group_name, 
                service.hostname, 
                service.port);
    }
    
    Ok(())
}`}
/>

<Callout type="info">
  Returns an empty vector if no services exist in the specified group.
</Callout>

## Return value

- **Type:** <code>Result&lt;Vec&lt;Service&gt;, Status&gt;</code>

All returned services belong to the specified group and contain their runtime information.
