---
title: Services
description: Getting Started with the Services PoloCloud
icon: Server
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';
import { Callout } from 'fumadocs-ui/components/callout';
import { Server, Search, Settings, Zap, Database, Terminal } from 'lucide-react';

## Service Management

Services in PoloCloud represent individual Minecraft server instances running in your cloud infrastructure. The SDK provides powerful tools to discover, manage, and interact with these services.

<div className="text-center mb-12">
    <div className="w-20 h-1 bg-gradient-to-r from-transparent via-primary/50 to-transparent rounded-full mx-auto mb-6" />
    <div className="text-xl text-muted-foreground max-w-3xl mx-auto">
        Discover, manage, and interact with your Minecraft services programmatically
    </div>
</div>

## Initialization

Get access to the service provider using the PoloCloud SDK:

<Tabs items={['Kotlin', 'Java']}>
    <Tab value="Kotlin">
        <DynamicCodeBlock lang="kotlin" code='val serverProvider = PoloCloud.serverProvider()' />
    </Tab>
    <Tab value="Java">
        <DynamicCodeBlock lang="java" code='var serverProvider = PoloCloud.serverProvider();' />
    </Tab>
</Tabs>

## Starting Services

Boot new service instances with custom configurations:

<Tabs items={['Kotlin', 'Java']}>
    <Tab value="Kotlin">
        <DynamicCodeBlock lang="kotlin" code={`// Start a service with default group settings
val serviceSnapshot = serviceProvider.bootInstance("lobby")

// Start a service with custom configuration
val bootConfig = SharedBootConfiguration(
    minMemory = 2048,
    maxMemory = 4096,
    templates = listOf(lobbyTemplate),
    excludedTemplates = emptyList(),
    properties = mapOf("custom-property" to "value")
)
val customServiceSnapshot = serviceProvider.bootInstanceWithConfiguration("lobby", bootConfig)`} />
    </Tab>
    <Tab value="Java">
        <DynamicCodeBlock lang="java" code={`// Start a service with default group settings
ServiceSnapshot serviceSnapshot = serviceProvider.bootInstance("lobby");

// Start a service with custom configuration
SharedBootConfiguration bootConfig = new SharedBootConfiguration(
    2048,  // minMemory
    4096,  // maxMemory
    List.of(lobbyTemplate),
    List.of(),  // excludedTemplates
    Map.of("custom-property", "value")
);
ServiceSnapshot customServiceSnapshot = serviceProvider.bootInstanceWithConfiguration("lobby", bootConfig);`} />
    </Tab>
</Tabs>

## Stopping Services

Shutdown running service instances:

<Tabs items={['Kotlin', 'Java']}>
    <Tab value="Kotlin">
        <DynamicCodeBlock lang="kotlin" code={`// Shutdown a service by name
val shutdownSnapshot = serviceProvider.shutdownService("lobby-1")

// Shutdown using service instance
val service = serviceProvider.find("lobby-1")
service?.shutdown()`} />
    </Tab>
    <Tab value="Java">
        <DynamicCodeBlock lang="java" code={`// Shutdown a service by name
ServiceSnapshot shutdownSnapshot = serviceProvider.shutdownService("lobby-1");

// Shutdown using service instance
Service service = serviceProvider.find("lobby-1");
if (service != null) {
    service.shutdown();
}`} />
    </Tab>
</Tabs>

## Service Properties

<Callout type="info">
**Service Information**

Each service contains detailed information about its current state, performance metrics, and configuration.
</Callout>

| Property        | Type           | Description                                    |
| --------------- | -------------- | ---------------------------------------------- |
| `groupName`     | `String`       | The group this service belongs to              |
| `id`            | `Int`          | Unique service identifier within the group     |
| `state`         | `ServiceState` | Current state (STARTING, RUNNING, STOPPED)     |
| `type`          | `GroupType`    | Service type (SERVICE, SERVER, PROXY)          |
| `hostname`      | `String`       | Network hostname for connections               |
| `port`          | `Int`          | Network port for connections                   |
| `playerCount`   | `Int`          | Current number of connected players            |
| `maxPlayerCount`| `Int`          | Maximum number of players allowed              |
| `memoryUsage`   | `Double`       | Current memory usage percentage                |
| `cpuUsage`      | `Double`       | Current CPU usage percentage                   |
| `motd`          | `String`       | Message of the day displayed to players        |
| `properties`    | `Map<String, String>` | Custom service properties                 |
| `templates`     | `List<Template>` | Templates used for this service            |

