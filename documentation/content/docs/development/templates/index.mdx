---
title: Templates
description: Working with templates in PoloCloud using the Java SDK
icon: FileText
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';
import { Callout } from 'fumadocs-ui/components/callout';

## Template Provider

The `TemplateProvider` gives you access to manage and work with templates in your PoloCloud infrastructure. Templates define the configuration and files that are used when creating new services.

<div className="text-center mb-12">
    <div className="w-20 h-1 bg-gradient-to-r from-transparent via-primary/50 to-transparent rounded-full mx-auto mb-6" />
    <div className="text-xl text-muted-foreground max-w-3xl mx-auto">
        Manage and organize service templates for consistent and efficient service deployment
    </div>
</div>

### Initialization

<Tabs items={['Kotlin', 'Java']}>
    <Tab value="Kotlin">
        <DynamicCodeBlock lang="kotlin" code='val templateProvider = Polocloud.instance().templateProvider()' />
    </Tab>
    <Tab value="Java">
        <DynamicCodeBlock lang="java" code='SharedTemplateProvider&lt;Template&gt; templateProvider = Polocloud.instance().templateProvider();' />
    </Tab>
</Tabs>

<div className="text-center mb-12">
    <div className="w-20 h-1 bg-gradient-to-r from-transparent via-primary/50 to-transparent rounded-full mx-auto mb-6" />
    <div className="text-xl text-muted-foreground max-w-3xl mx-auto">
        Manage and organize service templates for consistent and efficient service deployment
    </div>
</div>

## Finding Templates

Discover and retrieve templates from your cloud infrastructure:

<Tabs items={['Kotlin', 'Java']}>
    <Tab value="Kotlin">
        <DynamicCodeBlock lang="kotlin" code={`// Find all templates
val allTemplates = templateProvider.findAll()

// Find a specific template by name
val lobbyTemplate = templateProvider.find("lobby-template")

// Async operations
val allTemplatesAsync = templateProvider.findAllAsync()
val lobbyTemplateAsync = templateProvider.findAsync("lobby-template")`} />
    </Tab>
    <Tab value="Java">
        <DynamicCodeBlock lang="java" code={`// Find all templates
List<Template> allTemplates = templateProvider.findAll();

// Find a specific template by name
Template lobbyTemplate = templateProvider.find("lobby-template");

// Async operations
CompletableFuture<List<Template>> allTemplatesAsync = templateProvider.findAllAsync();
CompletableFuture<Template> lobbyTemplateAsync = templateProvider.findAsync("lobby-template");`} />
    </Tab>
</Tabs>

## Template Properties

<Callout type="info">
**Template Information**

Each template contains essential information about its configuration, files, and usage in your cloud infrastructure.
</Callout>

| Property        | Type           | Description                                    | Access Method |
| --------------- | -------------- | ---------------------------------------------- | ------------- |
| `name`          | `String`       | The template's unique identifier               | Direct field |
| `size()`        | `String`       | Total size of all files in the template (human-readable) | Method |

## Template Management Examples

### List All Templates

<Tabs items={['Kotlin', 'Java']}>
    <Tab value="Kotlin">
        <DynamicCodeBlock lang="kotlin" code={`// Get all templates and display information
val templates = templateProvider.findAll()

templates.forEach { template ->
    println("Template: \${template.name}")
    println("  Size: \${template.size()}")
    println("---")
}`} />
    </Tab>
    <Tab value="Java">
        <DynamicCodeBlock lang="java" code={`// Get all templates and display information
List<Template> templates = templateProvider.findAll();

for (Template template : templates) {
    System.out.println("Template: " + template.name);
    System.out.println("  Size: " + template.size());
    System.out.println("---");
}`} />
    </Tab>
</Tabs>

### Find Template by Name

<Tabs items={['Kotlin', 'Java']}>
    <Tab value="Kotlin">
        <DynamicCodeBlock lang="kotlin" code={`// Find a specific template
val template = templateProvider.find("lobby-template")

if (template != null) {
    println("Found template: \${template.name}")
    println("Size: \${template.size()} bytes")
} else {
    println("Template not found")
}`} />
    </Tab>
    <Tab value="Java">
        <DynamicCodeBlock lang="java" code={`// Find a specific template
Template template = templateProvider.find("lobby-template");

if (template != null) {
    System.out.println("Found template: " + template.name);
    System.out.println("Size: " + template.size());
} else {
    System.out.println("Template not found");
}`} />
    </Tab>
</Tabs>

### Async Template Operations

<Tabs items={['Kotlin', 'Java']}>
    <Tab value="Kotlin">
        <DynamicCodeBlock lang="kotlin" code={`// Async template operations
templateProvider.findAllAsync().thenAccept { templates ->
    println("Found \${templates.size} templates")
    templates.forEach { template ->
        println("- \${template.name} (\${template.size()} bytes)")
    }
}

templateProvider.findAsync("lobby-template").thenAccept { template ->
    if (template != null) {
        println("Template found: \${template.name}")
    } else {
        println("Template not found")
    }
}`} />
    </Tab>
    <Tab value="Java">
        <DynamicCodeBlock lang="java" code={`// Async template operations
templateProvider.findAllAsync().thenAccept(templates -> {
    System.out.println("Found " + templates.size() + " templates");
    for (Template template : templates) {
        System.out.println("- " + template.name + " (" + template.size() + ")");
    }
});

templateProvider.findAsync("lobby-template").thenAccept(template -> {
    if (template != null) {
        System.out.println("Template found: " + template.name);
    } else {
        System.out.println("Template not found");
    }
});`} />
    </Tab>
</Tabs>

<Callout type="tip">
**Template Usage**

Templates are essential for consistent service deployment. Use them to define standard configurations, plugins, and files that should be included when creating new services.
</Callout>

The Template Provider gives you powerful tools to manage and organize your service templates for efficient and consistent cloud infrastructure deployment! 📁🚀
