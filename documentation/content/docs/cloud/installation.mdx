---
title: Installation
description: Get PoloCloud up and running on your server with these installation instructions.
icon: Package
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock';
import { Download, Globe, Network, RefreshCw } from 'lucide-react';
import { Step, Steps } from 'fumadocs-ui/components/steps';

## Installing PoloCloud

Choose your preferred installation method:

<Tabs items={['Local', 'Docker']}>
  <Tab value="Local">
    <div className="text-muted-foreground mb-4">
      Install PoloCloud directly on your local machine. Requires Java 17 or higher.
    </div>
    
    <div className="mb-4">
      <a 
        href="https://polocloud.de/download" 
        target="_blank" 
        rel="noopener noreferrer"
        className="inline-flex items-center gap-1.5 text-sm text-primary hover:text-primary/80 transition-colors"
      >
        <Download className="w-3.5 h-3.5" />
        Download PoloCloud
      </a>
    </div>
    
    <Tabs items={['Simple', 'Production']} defaultValue="Production">
      <Tab value="Simple">
        <div className="relative group">
          <DynamicCodeBlock 
            lang="bash" 
            code='java -jar polocloud-launcher.jar'
            className="rounded-lg border border-border/50 bg-muted/30"
          />
        </div>
      </Tab>
      <Tab value="Production">
        <div className="relative group">
          <DynamicCodeBlock 
            lang="bash" 
            code='chmod +x ./start.sh
./start.sh'
            className="rounded-lg border border-border/50 bg-muted/30"
          />
        </div>
      </Tab>
    </Tabs>
    
    <div className="text-muted-foreground mt-4">
      This command starts PoloCloud with the specified configuration mode.
    </div>
  </Tab>
  
  <Tab value="Docker">
    <div className="text-muted-foreground mb-4">
      Run PoloCloud in a Docker container. Requires Docker and Docker Compose.
    </div>
    
    <div className="text-muted-foreground mb-3">Pull the latest PoloCloud Docker image:</div>
    <div className="relative group">
      <DynamicCodeBlock 
        lang="bash" 
        code='docker pull ghcr.io/httpmarco/polocloud:latest'
        className="rounded-lg border border-border/50 bg-muted/30"
      />
    </div>
    
    <div className="text-muted-foreground mb-3 mt-4">Run PoloCloud in a Docker container:</div>
    <div className="relative group">
      <DynamicCodeBlock 
        lang="bash" 
        code='docker run -d --name polocloud -p 8080:8080 ghcr.io/httpmarco/polocloud:latest'
        className="rounded-lg border border-border/50 bg-muted/30"
      />
    </div>
  </Tab>
</Tabs>

<div className="max-w-4xl mx-auto px-4 my-20">
    <div className="h-[2px] bg-gradient-to-r from-transparent via-border/40 to-transparent" />
    <div className="h-px mt-1 bg-gradient-to-r from-transparent via-primary/30 to-transparent blur-sm" />
</div>

## Onboarding

<Steps className="space-y-8">
  <Step>
    <h3 className="text-xl font-bold text-foreground mb-4">Language Selection</h3>
    
    <div className="text-muted-foreground mb-4">
      Choose your preferred language for the PoloCloud interface and documentation. All languages are managed through Crowdin for community-driven translations.
    </div>
  </Step>

  <Step>
    <h3 className="text-xl font-bold text-foreground mb-4">gRPC Port Configuration</h3>
    
    <div className="text-muted-foreground mb-4">
      Configure the gRPC port for internal communication between PoloCloud components.
    </div>
    
    <div className="relative group">
      <DynamicCodeBlock 
        lang="bash" 
        code='Port: 8932 (Default)'
        className="rounded-lg border border-border/50 bg-muted/30"
      />
    </div>
    
    <div className="text-sm text-muted-foreground mt-2">
      You can change this port if needed, but 8932 is recommended for most setups.
    </div>
  </Step>

  <Step>
    <h3 className="text-xl font-bold text-foreground mb-4">Automatic Updates</h3>
    
    <div className="text-muted-foreground mb-4">
      Choose whether to enable automatic updates for PoloCloud components.
    </div>
    
    <div className="grid grid-cols-1 md:grid-cols-2 gap-3">
      <div className="bg-card/20 backdrop-blur-sm border border-border/30 rounded-lg p-3 hover:bg-card/30 transition-colors">
        <div className="flex items-center gap-2 mb-1">
          <div className="w-1.5 h-1.5 bg-green-500 rounded-full"></div>
          <span className="text-sm font-semibold text-foreground">Enable Updates</span>
        </div>
        <div className="text-xs text-muted-foreground">
          Automatic updates for security and features.
        </div>
      </div>
      <div className="bg-card/20 backdrop-blur-sm border border-border/30 rounded-lg p-3 hover:bg-card/30 transition-colors">
        <div className="flex items-center gap-2 mb-1">
          <div className="w-1.5 h-1.5 bg-orange-500 rounded-full"></div>
          <span className="text-sm font-semibold text-foreground">Disable Updates</span>
        </div>
        <div className="text-xs text-muted-foreground">
          Manual control for production environments.
        </div>
      </div>
    </div>
  </Step>
</Steps>